name: telegram-send
description: Send a message through a Telegram bot
handler: telegram-send.php
interpreter: php
input:
  type: object
  required: [token, chatId, text]
  additionalProperties: false
  properties:
    token:
      type: string
      title: Bots token
    chatId:
      type: string
      title: ChatID
    text:
      type: string
      title: Message text to send

output: ~
