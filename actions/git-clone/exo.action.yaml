name: git-clone
description: Git clone command
handler: git-clone.php
interpreter: php
input:
  type: object
  required: [url, directory]
  additionalProperties: false
  properties:
    url:
      type: string
      description: Git repository URL
    directory:
      type: string
      description: Path to git repository
output: ~
