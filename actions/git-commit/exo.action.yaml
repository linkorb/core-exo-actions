name: git-commit
description: Git commit command
handler: git_commit.php
interpreter: php
input:
  type: object
  required: [directory, message]
  additionalProperties: false
  properties:
    directory:
      type: string
      title: Path to git repository
    message:
      type: string
      title: Commit message
output: ~
