name: git-push
description: Git push command
handler: git-push.php
interpreter: php
input:
  type: object
  required: [directory]
  additionalProperties: false
  properties:
    directory:
      type: string
      title: Path to git repository
output: ~
